import 'package:flutter/foundation.dart';

enum ViewState { Ideal, Busy }
enum AuthState { Login, Signup }
enum DialogType { warning, error, success }
enum ExerciseType { chest, back, leg, arm, abs, shoulder }
